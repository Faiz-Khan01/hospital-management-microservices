spring:
  application:
    name: prescription-service

  datasource:
    url: jdbc:mysql://localhost:3306/prescription_db
    username: root
    password: Password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8084